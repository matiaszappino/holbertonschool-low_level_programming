#include <stdio.h>
/**
 * main - prints name of the file
 *
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int main(void)
{

	printf("%s\n", __FILE__);
	return (0);
}
